describe('TaskService', () => {
	describe('#create', () => {
		describe('with invalid data', () => {
			it('should return error', () => {
				console.log('here');
			});
		});

		describe('with valid data', () => {
			it('should return the task created', () => {
				console.log('here');
			});
		});
	});
});
